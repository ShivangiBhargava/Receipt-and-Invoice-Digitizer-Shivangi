# Receipt and Invoice Digitizer
This project builds a system that automatically scans, extracts, and digitizes information from receipts and invoices using OCR(Optical Character Recognition) and NLP(Natural Language Processing)-based field extraction.
